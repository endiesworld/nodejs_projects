// Environments properties are defined in the console before the command to run the
// process/script is written. while argv[2] value are entered after the run/start
// script

// console.log("process.env:", process.env);
console.log("process.env.NODE_ENV: ", process.env.NODE_ENV);